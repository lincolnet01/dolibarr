apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: __CUSTOMER_NAMESPACE__
  labels:
    app: mariadb
    customer: __CUSTOMER_NAME__
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: mariadb
